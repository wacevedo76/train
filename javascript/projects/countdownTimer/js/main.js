const hoursContainer = document.querySelector('#cdHours');
const minutesContainer = document.querySelector('#cdMinutes');
const secondsContainer = document.querySelector('#cdSeconds');

function getCurrentTime() {
  const startTime = new Date();
  const hours = startTime.getHours(); 
  const minutes = startTime.getMinutes();
  const seconds = startTime.getSeconds();
  
  return [hours, minutes, seconds]
} 
function setTimer(arg)

function startTimer() {
  let hours = parseInt(hours.textContent)
}
